# The project already has sample public and private keys.

- In production: You must generate your own and save them in a secure storage. These keys must never be stored in a git repository which is accessible to audience not intended for it. 